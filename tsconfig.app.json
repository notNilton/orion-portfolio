{
  "compilerOptions": {
    /* Define o target para ES2020, garantindo compatibilidade com features recentes */
    "target": "ES2020",

    /* Habilita o uso de 'define' para inicialização de campos de classe */
    "useDefineForClassFields": true,

    /* Inclui bibliotecas padrão para ES2020, DOM e DOM.Iterable para compatibilidade com o navegador */
    "lib": ["ES2020", "DOM", "DOM.Iterable"],

    /* Define o formato de módulo para ESNext, adequado para bundlers como Vite */
    "module": "ESNext",

    /* Ignora a verificação de bibliotecas de tipo para melhorar a performance */
    "skipLibCheck": true,

    /* Modo de Bundler */
    "moduleResolution": "bundler",  // Resolve módulos de forma otimizada para bundlers
    "allowImportingTsExtensions": true,  // Permite importar arquivos com extensão .ts
    "isolatedModules": true,  // Garante que cada arquivo seja tratado como módulo separado
    "moduleDetection": "force",  // Força a detecção de módulos
    "noEmit": true,  // Desabilita a emissão de arquivos JavaScript

    /* Configuração para React */
    "jsx": "react-jsx",  // Define a sintaxe JSX compatível com React 17+

    /* Linting e verificações estritas */
    "strict": true,  // Habilita todas as verificações estritas do TypeScript
    "noUnusedLocals": true,  // Marca variáveis locais não utilizadas como erro
    "noUnusedParameters": true,  // Marca parâmetros não utilizados como erro
    "noFallthroughCasesInSwitch": true,  // Previne erros ao esquecer um break em switch

    /* Otimização de Performance e Diagnóstico */
    "forceConsistentCasingInFileNames": true,  // Garante consistência de maiúsculas/minúsculas nos nomes dos arquivos
    "skipDefaultLibCheck": true  // Ignora a verificação das bibliotecas padrão para acelerar a compilação
  },
  
  /* Inclui todos os arquivos TypeScript da pasta src */
  "include": ["src"]
}
