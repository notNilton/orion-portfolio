// src/components/Header.tsx
import React from 'react';
import '../styles/Header.scss'; // Importando o arquivo de estilo específico, se necessário

const Header: React.FC = () => {
  return (
    <div className="header">
      light dark
    </div>
  );
};

export default Header;
